{
  "_comment": "Category-B (weak-copyleft) dependencies allowed by exception under P17 / ADR-0004. Each entry is a deliberate, isolated, reviewed allowance — NOT a place to silence Cat-X (GPL/AGPL/source-available). `package` is matched by exact name OR name-prefix (covers per-platform variants). gate:licenses fails on any Cat-B dep not listed here.",
  "categoryB": [
    {
      "package": "@img/sharp-libvips",
      "license": "LGPL-3.0-or-later",
      "category": "X",
      "reason": "libvips native binary behind `sharp`. FINOS and ASF both classify LGPL as Category X, so this is NOT an allowed dependency — it is an OPTIONAL npm dependency that is never installed into any published artifact. It enters two ways: `next` declares `sharp` as an optionalDependency (clients/terminal), and `@huggingface/transformers`' IMAGE pipeline does the same (@vexa/mixed-pipeline). Both lanes omit it at install time.",
      "disposition": "Terminal image: NOT SHIPPED — clients/terminal/Dockerfile prunes node_modules/@img/sharp-libvips-* after `npm ci --omit=dev` (next/image is unused; `--omit=optional` was rejected because it also drops @next/swc-*, verified on a build host 2026-09-19). Bot image: STILL SHIPPED — @huggingface/transformers imports sharp at module load, so the binary cannot be omitted without a lazy-load change upstream or a fork; this is an OPEN Category-X blocker for the FINOS transfer, not an exception. Lockfile entries remain because the optional dependency is still declared upstream.",
      "scope": "clients/terminal (next -> sharp): pruned. @vexa/mixed-pipeline (@huggingface/transformers -> sharp): shipped, blocker open.",
      "approved": "ADR-0004 (revision pending: LGPL moves B -> X)"
    },
    {
      "package": "lightningcss",
      "license": "MPL-2.0",
      "reason": "Native CSS transform/minification engine used by the Next/Tailwind toolchain. MPL applies to the unmodified dependency files, not to Vexa source or generated CSS.",
      "disposition": "Logged exception; dependency remains an isolated build/runtime package managed by pnpm.",
      "scope": "frontend build pipeline and Next CSS handling",
      "approved": "ADR-0004"
    }
  ]
}
