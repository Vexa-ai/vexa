Python package for RTTM parsing, candidate execution, corpus DER, and synthetic fixtures.
Public API: `main`, `read_rttm`, `score`, `generate_fixtures`; command contract: `__main__.py`.
Uses standard-library process/audio tools and pyannote.metrics/core; candidates exchange RTTM files.
