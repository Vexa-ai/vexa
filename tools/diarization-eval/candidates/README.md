Discoverable candidate directories; every executable `run.sh` accepts input WAV and output RTTM paths.
The parent harness supplies `PYTHON` and `DIAR_REF_RTTM`; only ref and permute use the reference.
