# Build the four ZAKI Minutes deployment images from this fork.
#
# PUBLISH ON MERGE, PROMOTE BY HAND (owner ruling 2026-07-27). Publishing is not deploying:
# this produces immutable sha-pinned inputs and activates nothing. zaki-infra remains the ONLY
# thing that moves a tag into a cluster, by a reviewed edit to a values file.
#
# This workflow used to be workflow_dispatch-only, and it was the single repo in the platform
# without build-on-merge (NULL-ALIS, zaki-prod x4, zaki-design-engine and zaki-chat-engine all
# build on push). The cost of the exception was measured on 2026-07-27: four launch-blocking
# fixes — #43 (capture grant must hash the navigation URL: every Zoom `?pwd=` and enterprise
# Teams `?context=` link was 422'd), #44 (a flaky participant read could make the bot leave a
# LIVE meeting), #45, #46 — sat merged and green for days with no image ever built. Nothing
# surfaced the gap, because in this repo "merged" and "shipped" were different states and only
# one of them was visible.
#
# NO `paths:` FILTER, deliberately. meeting-api and bot both build with the repository root as
# their Docker context, so almost any file can change those images. A narrow allowlist would
# silently skip real rebuilds and recreate the exact failure above. Only docs and licences are
# ignored. A run is ~8 minutes.
name: zaki-minutes-images

on:
  push:
    branches: [main]
    paths-ignore:
      - "docs/**"
      - "licenses/**"
      - "**/*.md"
  workflow_dispatch:

concurrency:
  group: zaki-minutes-images-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  packages: write

jobs:
  publish:
    name: Build and publish immutable ZAKI Minutes images
    runs-on: ubuntu-latest
    timeout-minutes: 180
    env:
      REGISTRY: ghcr.io
      IMAGE_OWNER: projectnuggets
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Resolve immutable source revision
        id: source
        run: |
          set -euo pipefail
          SOURCE_SHA="$(git rev-parse HEAD)"
          [[ "$SOURCE_SHA" =~ ^[0-9a-f]{40}$ ]] || {
            echo "::error::Expected a full lowercase Git SHA, got '$SOURCE_SHA'"
            exit 1
          }
          echo "sha=$SOURCE_SHA" >> "$GITHUB_OUTPUT"
          echo "tag=sha-$SOURCE_SHA" >> "$GITHUB_OUTPUT"

      - name: Free runner disk for the browser worker build
        # Third-party action pinned to its reviewed commit. Run it before registry login so it
        # cannot read the packages-write credential Docker stores in the runner configuration.
        uses: jlumbroso/free-disk-space@54081f138730dfa15788a46383842cd2f914a1be # v1.3.1

      - uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push the deployment image set
        env:
          SOURCE_SHA: ${{ steps.source.outputs.sha }}
          SOURCE_TAG: ${{ steps.source.outputs.tag }}
        run: |
          set -euo pipefail
          DIGESTS="$RUNNER_TEMP/zaki-minutes-image-digests.tsv"
          : > "$DIGESTS"

          publish_image() {
            local name="$1"
            local dockerfile="$2"
            local context="$3"
            local pull_base="${4:-true}"
            local image="${REGISTRY}/${IMAGE_OWNER}/zaki-minutes-${name}:${SOURCE_TAG}"

            local build_args=()
            if [[ "$pull_base" == "true" ]]; then
              build_args+=(--pull)
            fi
            DOCKER_BUILDKIT=1 docker build "${build_args[@]}" \
              --label "org.opencontainers.image.source=${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}" \
              --label "org.opencontainers.image.revision=${SOURCE_SHA}" \
              --label "org.opencontainers.image.title=zaki-minutes-${name}" \
              -f "$dockerfile" \
              -t "$image" \
              "$context"
            docker push "$image"

            local digest
            digest="$(docker buildx imagetools inspect "$image" --format '{{.Manifest.Digest}}')"
            [[ "$digest" =~ ^sha256:[0-9a-f]{64}$ ]] || {
              echo "::error::Could not resolve a registry digest for $image"
              exit 1
            }
            printf '%s\t%s\t%s\n' "$name" "$image" "$digest" >> "$DIGESTS"
          }

          publish_image admin-api core/identity/services/admin-api/Dockerfile core/identity/services/admin-api
          publish_image runtime core/runtime/Dockerfile core/runtime
          publish_image meeting-api core/meetings/services/meeting-api/Dockerfile .

          # The bot Dockerfile uses this local base. It is built from this source tree and is never
          # pushed or substituted with a public Vexa image.
          DOCKER_BUILDKIT=1 docker build --pull \
            -f core/meetings/modules/join/Dockerfile.env \
            -t vexa/meet-join-env:dev \
            core/meetings/modules/join
          # `vexa/meet-join-env:dev` is deliberately the locally built, source-controlled base
          # immediately above. `--pull` would ignore it and try (and fail) to fetch that private
          # local tag from Docker Hub, so bot alone opts out of the helper's base refresh.
          publish_image bot core/meetings/services/bot/Dockerfile . false

      - name: Write chart-ready digest manifest
        env:
          SOURCE_SHA: ${{ steps.source.outputs.sha }}
        run: |
          set -euo pipefail
          node <<'NODE'
          const { readFileSync, writeFileSync } = require("node:fs");

          const sha = process.env.SOURCE_SHA;
          const rows = readFileSync(`${process.env.RUNNER_TEMP}/zaki-minutes-image-digests.tsv`, "utf8")
            .trim()
            .split("\n")
            .filter(Boolean)
            .map((line) => line.split("\t"));
          if (rows.length !== 4 || rows.some((row) => row.length !== 3)) {
            throw new Error("expected digest rows for exactly four deployment images");
          }
          const images = Object.fromEntries(rows.map(([name, reference, digest]) => {
            const repository = reference.slice(0, reference.lastIndexOf(":"));
            return [name, {
              repository,
              tag: `sha-${sha}@${digest}`,
              digest,
              reference: `${reference}@${digest}`,
            }];
          }));
          writeFileSync("zaki-minutes-images.json", `${JSON.stringify({ sourceSha: sha, images }, null, 2)}\n`);
          NODE

          {
            echo "## ZAKI Minutes images"
            echo
            echo "Source SHA: \`${SOURCE_SHA}\`"
            echo
            while IFS=$'\t' read -r name reference digest; do
              echo "- \`${name}\`: \`${reference}@${digest}\`"
            done < "$RUNNER_TEMP/zaki-minutes-image-digests.tsv"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload immutable image manifest
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: zaki-minutes-images-${{ steps.source.outputs.sha }}
          path: zaki-minutes-images.json
          if-no-files-found: error
          retention-days: 30
