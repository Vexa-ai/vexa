# recording-players — L4 fixtures

The bundled fixture page + golden props the Playwright spec mounts the component against. Goldens are
shaped per `@vexa/dash-contracts`, so the view is exercised with the same shapes the live stack emits.

