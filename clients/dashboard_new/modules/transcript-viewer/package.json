{
  "name": "@vexa/dash-transcript-viewer",
  "version": "0.1.0",
  "description": "Presentational React transcript view for the modular dashboard. Props in, DOM out: renders attributed segments (speaker + text + time), a live indicator, and a search box over an injected TranscriptSegment[] (typed by @vexa/dash-contracts). No store, no fetch, no ws — the clean counterpart of the vendored dashboard's transcript-viewer with the coupling stripped. Inline-styled so it paints identically in a bare browser fixture and on the real stack.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc && node scripts/build-dist.mjs",
    "test": "playwright test --config e2e/playwright.config.ts",
    "test:headed": "playwright test --config e2e/playwright.config.ts --headed",
    "install:browser": "playwright install chromium"
  },
  "dependencies": {},
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.28.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.6.0"
  }
}
