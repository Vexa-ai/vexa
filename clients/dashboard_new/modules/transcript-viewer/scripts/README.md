# transcript-viewer — build scripts

Helper scripts for this brick (e.g. bundling the component into the L4 fixture page). Invoked by the
brick's `build`/`test` npm scripts; not part of the published front door.

