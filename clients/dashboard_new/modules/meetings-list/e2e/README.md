# meetings-list — L4 Playwright harness

The real-browser proof for this view brick: a static fixture page bundles the component and the spec
asserts its DOM contract in chromium. Run from the brick (`npm test`). Green here = the brick renders
for a real browser, not just in a unit test.

