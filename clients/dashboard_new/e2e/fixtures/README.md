# e2e fixtures — top-level L4

Bundles + goldens for the cross-brick L4 harness (`e2e/ws-render.spec.ts`): the dash-ws bundle and the
golden frames a fixture page renders through it. Shaped per the sealed ws.v1 contract.

