# layout — app chrome components

The dashboard shell: `app-layout.tsx` (the frame), `header.tsx`, `sidebar.tsx`. Composition only — these
arrange the feature components and `ui/` primitives; they own no business logic.
