# app/api/config 

`GET /api/config` — resolves the browser's `{apiUrl, wsUrl, authToken}` at runtime via `@vexa/dash-config` (the loopback/tunnel/gateway-direct vs same-origin decision + the single token order).
