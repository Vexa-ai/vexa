# minutes tests

Behavioral tests for the minutes-mode shell. `pagesWidth.test.ts` pins the document rail's normal
width and verifies that dragging can use all available viewport space while preserving the minimum
conversation and document widths.
