{
  "name": "@vexa/extension",
  "version": "0.1.0",
  "description": "In-tab Google Meet transcription — the live page-audio capture driver for the desktop. Captures per-participant audio in the user's own meeting tab and streams capture.v1 to the desktop ingest. No bot, no admission.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
