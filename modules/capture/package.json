{
  "name": "@vexa/capture",
  "version": "0.1.0",
  "description": "Isolated meeting-capture layer — emits capture.v1 (per-speaker audio + events).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "check:isolation": "node scripts/check-isolation.js",
    "test": "echo '(goldens dropped — reorg in progress)'"
  },
  "devDependencies": { "typescript": "^5.4.0", "tsx": "^4.0.0" }
}
