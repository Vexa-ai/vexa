{
  "name": "vexa-extension",
  "version": "0.1.0",
  "description": "In-tab Google Meet transcription — captures audio in the user's own meeting tab and streams it to the Vexa ingest server. No bot, no admission.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.8.2"
  }
}
