# Canonical tests3 validation account contract.
#
# This account is used by local/helm human-gate and smoke harnesses. Keep the
# bot limit here so parallel validation capacity does not drift across setup
# scripts, runtime checks, and human-gate evidence.
TEST_ACCOUNT_EMAIL=test@vexa.ai
TEST_ACCOUNT_NAME="Test User"
TEST_ACCOUNT_MAX_CONCURRENT_BOTS=3
