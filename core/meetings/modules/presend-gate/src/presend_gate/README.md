# `presend_gate`

Import from the package root — `presend_gate.gate`, `.evaluate`, `.route_recipients`,
`.from_transcript_payload`, `.Policy`, `.Outcome`. The submodule split is measurement
(`signals`) vs judgement (`policy`) so a threshold argument never turns into a
measurement change; see [`../../README.md`](../../README.md) for the why.
