# chat-door/src — the package root

One package, `chat_door/`, on `PYTHONPATH=src` (the repo's Python-service layout). See
[`chat_door/README.md`](chat_door/README.md) for the module map and
[`../README.md`](../README.md) for what the service is.
