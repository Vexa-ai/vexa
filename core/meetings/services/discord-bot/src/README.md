# src — discord-bot source root

Holds the `discord_bot` Python package (the `pythonpath` for tests, per `pyproject.toml`). The package is the front door; import from `discord_bot`, never a deep module path (P6).
