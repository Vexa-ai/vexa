# Golden vectors

ALLOY: these examples pin the active and waiting producer states, green and muted
aggregates, and all three status-response branches. The filename prefix is the
schema `$def` validated by `../validate.mjs`.
