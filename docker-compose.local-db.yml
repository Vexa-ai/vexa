# Postgres and DB dependencies now live in docker-compose.yml.
# Use only:
#   docker compose up -d
#
# This file is kept so older commands that passed -f docker-compose.local-db.yml
# still work without merging a second postgres definition.
