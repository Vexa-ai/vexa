- **Browser sessions are a first-class capability again — `POST /bots {"platform":"browser_session"}` returns 201 (#816).** The 0.12 core now serves the browser-session create/stop contract that regressed at the 0.10→0.12 cutover: intake mints a `bs-<hex>` id + an unguessable session token, marks the row ACTIVE immediately, and spawns a dedicated `browser-session` runtime workload (image `${BROWSER_IMAGE}`, its own profile — not a meeting bot); `DELETE /bots/browser_session/{id}` tears the workload down immediately with zero stop delay. A session addresses itself, so supplying `native_meeting_id`/`meeting_url` is a typed 422, and a real meeting with no URL/id still 422s (the #794 guarantee is intact). Replaces the interim #829 refusal.
