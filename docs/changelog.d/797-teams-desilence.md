- **MS Teams: a speaker signal the resolver cannot name is now REPORTED, not silently dropped (#797, #853).** When Teams' voice-outline fires but the display-name resolver returns empty, the bot log now says so — naming which name-selector strategies were tried and a sanitized (no-user-text) description of the element — rate-limited so a drifted selector cannot flood. A blind voice-outline watcher that never sees a speaker now emits `NO ACTIVE SPEAKER seen in Ns`, matching Zoom's report so one log grep catches both platforms. Selector modernization still needs a live session and is out of scope here.
