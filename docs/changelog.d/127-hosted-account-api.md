Hosted Account and API-key management now bind identity through the stock `/auth/me` contract and consume the stock v0.12 Admin API’s token metadata and strict mint contracts. Structured validation failures render as actionable text, token secrets remain visible only on successful creation, and the dashboard image installs the reviewed lockfile dependency graph.
