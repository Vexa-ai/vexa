- **Codex can now run workspace-agent turns alongside Claude Code (#1313).** Operators can select
  the Codex app-server harness, use a read-only host subscription login, resume conversations after
  worker recreation, and steer an active turn without waiting for it to finish.
