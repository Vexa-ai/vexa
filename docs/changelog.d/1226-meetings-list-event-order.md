- **Changed (API): your meetings list is ordered by when the meeting happened (#1226).** `GET /meetings` previously ordered by row creation, so a meeting imported from a calendar after it ran could sort above one that happened later. The list now orders by event time, and meetings that have not reached a terminal state stay pinned at the top where you are looking for them. See [Meetings](/api/meetings).
