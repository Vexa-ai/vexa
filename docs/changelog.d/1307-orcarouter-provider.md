- **Added (self-hosted):** the meeting copilot's completion layer can now target **OrcaRouter** (`VEXA_LLM_PROVIDER=orcarouter`) — a named OpenAI-compatible gateway with a built-in default endpoint, so deployments opt in with just the provider key and model (no base URL to type). The existing `openai-compat` path still covers OpenRouter/Ollama/vLLM/LM Studio/any custom endpoint, and `VEXA_LLM_BASE_URL` still overrides for self-hosted or proxied installs.
