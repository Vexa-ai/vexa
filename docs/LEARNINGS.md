# LEARNINGS — the running ledger

Every **unexpected** objective-closure (reality ≠ expectation) is interpreted **with the human** as a
*learning*, and promoted **twice**: to the **architecture** (a principle + its gate + an ADR) *and* to
this log (ADR-0017/0018). ADRs capture the *decision*; this captures the *surprise → root-cause →
promotion* narrative, chronologically — including learnings that became a *practice*, not a numbered
principle.

| # | Date | Surprise (expected → actual) | Root-cause / gap | Learning | Promoted to |
|---|---|---|---|---|---|
| 1 | 06-19 | "no transcript" → STT **HTTP 402** out-of-balance, swallowed silently | dependency failure had no observable surface | **fail loud + attributable** — surface every dependency fault | P18 · ADR-0010 · `gate:fault-surfacing` |
| 2 | 06-19 | gate-green lane "done" → gmeet **never tested** (solo), youtube intermittent, STT dead | structural green (L1–L3) read as "works" | **prove at the altitude of the claim** — user-facing needs L4 | P19 · ADR-0011 · `gate:eval-baseline` (ADD) |
| 3 | 06-19 | `/transcripts`·`/recordings`·`/ws` assumed safe → **wide open** to any localhost reader | `canAccess` seam designed (ADR-0003) but never wired → rotted | **complete mediation** — authorize every access, default-deny | P20 · ADR-0012 · `gate:access` (ADD) |
| 4 | 06-19 | "Listening — capturing **0 stream(s)**" → state asserted from Start, no frames flowing | UI state from *intent*, not observed evidence | **report state from evidence, not intent** | P21 · ADR-0013 · `gate:client-liveness` |
| 5 | 06-19 | eval `capture` called a **healthy gmeet "unhealthy"** | instrument judged ch999 (mixed lane); gmeet uses per-participant ch0..N — wrong success-definition | **instruments are cheap but approximate** — can mis-define / mis-interpret | ADR-0014 · fixed `eval/src/capture.mjs` |
| 6 | 06-19 | "I updated the balance" / "gmeet doesn't transcribe" → 402 (wrong account) / solo tape | trusted human reports as definitive | **human is ground truth but fallible** — cross-validate both ways | ADR-0014 |
| 7 | 06-19 | audit subagent ran on the dirty tree → **destroyed the uncommitted `bot/` brick** (data loss) | write-capable agents on a dirty working tree | isolate audits read-only / in a worktree; **uncommitted work is at risk** | *practice* (candidate: single-source-of-truth) |
| 8 | 06-19 | "both vexa-secrets STT tokens = `32c5…`" → stage (44-char) ≠ prod (32-char); stage **403** | two secret sources drifted silently | single source of truth, **committed & reconciled** | *candidate principle* (held, single instance) |
| 9 | 06-19 | improvising next steps → drift risk without a path | no standing plan / mode discipline | **plan ⇄ execute**; a current plan always exists | ADR-0015 · RELEASE-PLAN.md |
| 10 | 06-20 | "P2-2a done — L3-validated" → hid that tests use **fakes**, no real broker | a report shipped an *evaluation*, not facts | **report facts, not evaluations** | ADR-0016 |
| 11 | 06-20 | report had no reference frame → result not assessable | objective not stated | **goal vs objective**; a report assesses *result vs objective* | ADR-0017 |
| 12 | 06-20 | learnings lived only in scattered ADRs → no running ledger | learnings not logged | **learnings earned with the human, promoted to architecture + this log** | ADR-0018 · this file |
| 13 | 06-20 | a `meeting-api` package **appeared mid-session** — built by *another chat* sharing this working tree (this session nearly overwrote it) | concurrent chats on one shared tree don't isolate uncommitted state; no branches/PRs (audit) | **one git worktree per chat, integrate via PR** — never two chats on one working tree; never adopt another chat's uncommitted files | ADR-0019 · §8 |
| 14 | 06-20 | after an **expected** closure I **stopped** ("to respect cadence") instead of continuing | the loop is a heartbeat, not a reason to stop; the next objective is already in the plan | **expected → continue the next *planned* objective autonomously; unexpected → update the plan** | ADR-0017 · §8 |
| 15 | 06-20 | L4 re-test: panel showed **"Listening — capturing 0 stream(s)"** while YouTube transcribed 21 segs (new build) | P21 fixed the *status* (`capturing` earned by frames) but a **sibling field** — the `streams` *count* — stayed decoupled, lying right next to the fix | when a state field is made to reflect reality, **audit its siblings** — a fix can leave an adjacent display still unearned | sidepanel fix (reinforces P21) |
| 16 | 06-20 | banked a gmeet **"L4 PASSED"** on `misattr=0` — the human asked "my mic = Speaker, right? don't we need speaker-bots to *definitely* validate?" | the pass was **vacuous**: no NAMED speakers, so attribution was never *exercised* (and the mic was even mislabeled 'Speaker'); the definitive test (speaker-bots eval) was **unplanned** | a metric passed without being **exercised** is not validated — plan the test that exercises each claimed metric (P19: prove at the altitude of the claim) | BASELINE caveat · RELEASE-PLAN (speaker-bots eval) · mic→"You" fix |
| 17 | 06-20 | speaker-bots eval (2 named bots): expected clean gmeet attribution → the **host's name leaked onto a remote channel** — `ch-0` got `Dmitriy Grankin`×3 (content-proven: "This is Anna." labeled the host), while `ch-1` (Zoya) stayed 7/7 clean | self-tile exclusion was **only** the `data-self-name` marker, re-read every scan; Meet drops it transiently → host tile reclassified *remote* → its glow leaked via the binder, which had **no self-awareness** | the **self identity is structural** (self audio = the mic, never a remote `<audio>`) — pin a **sticky self-name the binder refuses to bind**, don't trust a per-scan DOM marker alone | `gmeet-channel-binder` `selfName` backstop + `setSelfName` + `gmeet-speakers.onSelf`; **`gmeet-channel-binder.test.ts`** (leak reproduced, 10 L2) under `gate:node`; BASELINE attribution row |
| 18 | 06-20 | `judge.py` scored attribution **precision 64% (9 wrong)** → cross-checking by **content**, ~5 of the 9 were *correctly-labeled* segments shifted into the adjacent truth window | the instrument aligns to ground truth with a **single global offset** (`L=3.4s`); per-turn `/speak` playback latency **varies**, so boundary segments mis-bucket | a time-aligned attribution score **over-counts** — cross-validate with the **content oracle** (clips self-ID); the instrument is cheap-but-approximate (validation economy) | BASELINE method note · `judge.py` follow-up (per-turn content anchoring) |
| 19 | 06-20 | the **human** flagged: "I can't see the expectation–reality loop running consistently" — expected each hop to be *predict → do → compare → classify*; **actual** = it degraded to "do → report" (no `Expected` stated before acting) | the loop was **codified** (ADR-0014 · §8 · AGENTS step 1 "expect first") but had **no visible forcing function** — under execution momentum the predict-before + verdict-after silently eroded, so **nothing could register as *unexpected* → no learning fired** | a principle with no **visible artifact** drifts — make the loop a **mandatory, written objective ledger** (Objective/Expected before · Actual/Verdict after); **no `Expected` ⇒ not in the loop**; the human reading the ledger IS the gate | **AGENTS.md "objective ledger"** (forcing function) · this log |
| 20 | 06-20 | building the "rough" 0.12 I wrote a **from-scratch sqlite meeting-api** + invented response shapes — the human stopped me: "**rough ≠ half-done**; cloud uses **postgres** (sqlite only desktop); **reuse main's images**; we already have a carved bot (`vexa-bot_new`) + extension" | I built v0.12 from the contracts + my assumptions instead of **grounding in main's real running system** (the 0.10.6.3 deploy/images/postgres + the existing carved bot/extension) — I reinvented, badly, what already exists | **rough = full scope built *properly* by reusing the real services, not stubs** — ground every refactor in the running system first; the existing stack IS the spec (keep-vs-change before build) | RELEASE-PLAN (deployment model + keep-vs-change) · this log |
| 21 | 06-20 | I found the v0.12 bot's capture→pipeline bridge **unbuilt** (`noopPipeline`) and **STOPPED + escalated it as UNEXPECTED**; the human corrected: "the new bot was mid-**scaffold**, unfinished — your job is to **finish** it; nothing unexpected; do **not** stop, stay largely autonomous" | I **conflated "work remaining" with "UNEXPECTED"** — an unfinished scaffold I'm meant to complete is *expected work*, not a contradiction that warrants stopping the loop | **UNEXPECTED = reality *contradicts* the plan** (a frozen field can't be met, an assumption is false) — **NOT** "the thing isn't built yet." Don't stop/escalate for remaining scaffolding; **finish it autonomously.** Reserve STOP for genuine contradictions | **AGENTS.md** (UNEXPECTED ≠ unfinished work) · this log |
| 26 | 06-21 | the full O6 matrix had stalled for ~30 cycles on "need a meeting / need speaker tokens." Two patterns cracked it: **(a)** a **real public/recurrent meeting** clears the admit gate AND supplies real audio at once (Zoom, Teams) — no human admit, no speaker tokens; **(b)** a **controlled meeting** (the human's own) validates **speaking** — trigger via acts.v1 `{action:'speak',text}` PUBLISH to `bot_commands:meeting:{id}`, the human confirms audible. Also: the **per-participant (gmeet) lane can't transcribe the bot's OWN speech** (its mic isn't a remote `<audio>`), but the **mixed lane (Teams/Zoom) can** (it captures the meeting's combined audio incl. the bot's TTS) | the historical blockers (admit wall #22, speaker-token wall) were artifacts of using PRIVATE meetings + a token-driven harness; **public meetings + the human's own meetings sidestep both** — and they exercise REAL audio (better than synthetic clips). The gmeet-vs-mixed self-audio asymmetry is a capture-topology consequence, not a bug | **matrix validated LIVE** across Meet·Zoom·Teams: join · transcription · attribution (gmeet `"Dmitriy Grankin"` perfect — Learning #17 fix confirmed live; mixed misattr=0) · speaking (heard) · recording-acquire. BASELINE ⭐ live-bot rows (zoom/teams/meet). **Public + controlled meetings are the standing O6 validation recipe.** |
| 25 | 06-20 | first LIVE Zoom run (real public meeting — bot **auto-admitted, no human**): **zero transcription** despite the mixed lane being wired. The bot routes Zoom→mixed lane (single stream→pyannote), but Zoom exposes **no per-participant `<audio>` in the DOM**, so `createMixedAudioCapture(w.__vexaRemoteStream)` got `undefined` — the hook that mirrors WebRTC audio was never installed and the mixed stream was never built | the mixed lane's capture **SOURCE** (`installRemoteAudioHook` → mirror each remote WebRTC track → combine into one stream) was unwired for the bot — a **third** browser-resident scaffold gap (after capture #23, recording #24); the hook MUST run **pre-navigation** (addInitScript) to patch `RTCPeerConnection` before Zoom builds it | **fix:** addInitScript `installRemoteAudioHook` for zoom/teams + combine the mirrored `__vexaCapturedRemoteAudioStreams` into one live dest stream (rescan for late speakers) → mixed lane. **Zoom VALIDATED LIVE** — 39 real-speech segments · **misattr=0** · overseg 18% ≤ baseline · recording-acquire 12 chunks. Public meetings break the admit+audio gate at once. (`capture-bridge.ts` · BASELINE zoom row) |
| 24 | 06-20 | after fixing #23's capture bug I **audited the sibling browser-resident leg** (recording) before its live run — and found `recording.chunk()` (the MediaRecorder→recording.v1 ingress) was **NEVER wired**: `createBotRecordingSink` was passed to the orchestrator for `close()`, but no MediaRecorder fed it, so `recordingEnabled=true` would capture **nothing** (the `@vexa/record-chunker` brick existed but was unused — an unfinished scaffold, like the capture bridge in #21) | the browser-resident legs (capture · recording · speak) are all L4-only and were scaffolded independently; fixing one (#23) doesn't fix its siblings — each had to be wired + started post-admission separately. Caught by **auditing the siblings of the fix** (Learning #15) + **finishing the scaffold** (Learning #21) instead of waiting for the live run to surface it | **fix:** bundle `createRecordingTap` into VexaBrowserUtils + `startRecording()` in capture-bridge (base64 chunk→`recording.chunk`, key matches the orchestrator's `platform/native`), started in `pipeline.start()` post-admission. Build+bundle+9 gates green; L4 pending the live run. (`build-browser-utils.mjs` · `Dockerfile` · `capture-bridge.ts` · `index.ts`) |
| 23 | 06-20 | first LIVE capture run (bot admitted+active on a real Meet): **zero transcription**. Two real bugs, found only by going live + adding page-console forwarding: **(a)** `startCaptureBridge` ran at composition time on the **blank pre-navigation page** (no `window.VexaBrowserUtils`, no `<audio>` elements), and the `goto` to the meeting URL **destroyed that context** → capture never attached; **(b)** the per-stream `AudioContext` was created but **never resumed** + no `--autoplay-policy=no-user-gesture-required` → context stayed **suspended** → worklet never fired → zero PCM | the earlier "capture-injection proven" probe gave **false confidence** — it evaluated on an already-navigated page, not in the real join→navigate→admit sequence; and the whole capture path is browser-resident (no unit test reaches it). **Fix (a):** defer `startCaptureBridge` to `pipeline.start()` (orchestrator calls it post-admission, on the live meeting page). **Fix (b):** `ctx.resume()` + the autoplay flag. After both: `ctx=running`, worklet posts frames continuously on all 3 streams (`seen=250→750`) — **capture pipeline proven end-to-end live**; only audible-speech→STT remains (the captured streams were digitally silent — no live speaker). | `index.ts` (deferred capture) · `gmeet-capture.ts` (`ctx.resume()` + frame diag) · `join/browser-args.ts` (autoplay flag) · `capture-bridge.ts` (page-console obs) · this log |
| 27 | 06-21 | the **mock-bot L3 lane** (the carved bot's REAL orchestrator+adapters, only Join+Pipeline faked, spawned by the REAL compose stack) caught **4 backend↔bot integration bugs the live O6 (L4) run never surfaced**: (1) `bot_spawn` emits `BOT_CONFIG` but the sealed `invocation.v1` + carved bot read `VEXA_BOT_CONFIG` → the carved bot got NO config (died `validation_error`); (2) the unified meeting-api advanced only an **in-memory** FSM → DB `meetings.status` frozen at `requested` (not queryable/durable); (3) the bot's transcript adapter XADDs a **flat** `{...segment}` but the collector's `ingest` REQUIRES `{meeting_id, segments:[…]}` → transcripts silently dropped (`return 0`); (4) the api.v1 `DELETE /bots` HTTP route is **unmounted** (stop logic exists) | O6 validated the bot at L4 but **bypassed the control plane**: it read the raw `transcription_segments` stream directly (never exercised the collector) and used the **legacy published image** (reads `BOT_CONFIG`, masking #1) — so the backend↔bot SEAMS were never tested end-to-end. Only the full real stack + a contract-faithful mock exercises them | **a backend needs its own L3 lane through the REAL stack** — an L4 bot run that reads side-channels does NOT validate the control-plane seams. SoC (backend ⊥ bot via a mock that REUSES the real adapters) is the instrument; the mock's fidelity (only the two heavy ports faked) is what makes its findings real bugs, not artifacts | `gate:compose`+`MOCK_BOT` (`deploy/compose/tests/mock_scenarios_test.py`) · fixes: `bot_spawn/invocation.py` (#1), `app.py`+`bot_spawn/{ports,adapters,fakes}.py` (#2), `bot/src/adapters/transcript-redis.ts` (#3); #4 flagged (DELETE /bots task) · this log |
| 28 | 06-21 | the live B:V1 run BLOCKED on STT for the whole session: prod `TRANSCRIPTION_SERVICE_TOKEN` → **402 out-of-balance** (0 min, user `2280905@gmail.com`), then `TRANSCRIBER_API_KEY_INTERNAL`/`REMOTE_TRANSCRIBER_API_KEY` → **403 invalid** — all discovered only AFTER spawning the real bot + a human admit, **mid-execution**, never at planning | the execution-target registry (ADR-0020) *recorded* the STT resource but planning only **RESOLVED** it (the env var/token exists), never **VALIDATED** it (does the token authenticate? is the per-user account FUNDED?). A 1-call `GET /balance` would have surfaced the 402/403 in seconds, in planning — not after a 4-hour run + repeated human re-admits | **planning must RESOLVE *and VALIDATE*** — a resource is cleared only when a **cheap preflight PROVES it works** (reachable · authenticates · **funded/in-balance**); **surface the failure model in planning, not at execution** (validation economy §8.2). The bot's **P18 fault-surfacing** (402/403 as typed, attributed faults) is what made the live diagnosis instant — fail-loud paid off | **AGENTS.md** §"Where work runs" (resolve **+ validate**) · `execution-targets.v1` gains a `validate` field + the `/balance` STT preflight · this log |
| 22 | 06-20 | drove the **actual v0.12 bot** into a live Meet on bbb (autonomous, no human) → it joined the **lobby** and **timed out unadmitted** (`joining→awaiting_admission→failed(join_failure)`); also bbb has **no S3 Google profile** (only MINIO) so auth-auto-admit isn't primed there | O6's live audio legs (transcription/attribution/recording/speaking/webhooks) need an **admitted participant the bot cannot admit itself** — **guest-join needs a human to click admit; auth-auto-admit needs a logged-in profile *and* an owned/org meeting** — neither is bypassable by autonomy alone | this is the **anticipated 🧑 gate** (not UNEXPECTED): autonomy's job at a true human gate is to **prove every cell that does *not* need the human** (build · join-to-lobby · capture-injection · lifecycle state-machine — all ✅ live on bbb) + **prime the one-command live run**, then **hand off the admit + PushNotify** — looping past a human gate is wasted motion (complements #14/#21: don't stop *early*, but at a real gate hand off cleanly, don't spin) | RELEASE-PLAN O6 (autonomous-proofs ledger + handoff) · this log |
| 29 | 06-22 | a human-gated dashboard `Join` failed (**429**). I root-caused it (transient: my own max-bots cap-test had seeded 5 active rows that collided with the click) AND found+fixed a real latent bug it exposed (`update_meeting_status` returned `_row_to_dict(m)` without `await db.refresh()` → reading the server-`onupdate` `updated_at` threw `MissingGreenlet`, **silently** breaking lifecycle DB-persistence + webhook delivery via a best-effort try/except, and 500'ing the stop route) — but my reply still ended "**retry whenever you're ready**", handing the path back to the human | I treated the **human as a retry/probe mechanism** — re-spending the scarce + fallible oracle to *discover* whether the fix worked, exactly what a cheap instrument should catch. A human gate is not a coin-flip retry button; "try again" leaks un-de-risked work onto the most expensive resource | **the human is NOT a retry mechanism.** On a **human-red**, fully elaborate the failure: **reproduce WITHOUT the human, root-cause completely, fix, and prove it green on a cheap instrument** (a dry spawn / API call / replay that re-creates the human's exact path) BEFORE the next human delivery attempt — each human attempt must be *earned* by a green instrument. ("Retry whenever you're ready" is the smell.) Corollary: a fake-backed unit test can't catch a real-driver bug (`MissingGreenlet` only appears against real SQLAlchemy) — **self-validate on the real stack**, not the fake | **AGENTS.md** §8 step 3 ("the human is NOT a retry mechanism") · fix: `bot_spawn/adapters.py` (`db.refresh` before `_row_to_dict`) · this log |
| 30 | 06-22 | the dashboard showed **ZERO meetings** for `test@vexa.ai` even though 2 meetings existed (user 1) and `GET /meetings` returned them via the same token. The live dashboard walk surfaced it; the backend's own api-surface tests + the mock harness never did | the vendored dashboard's meetings-list proxy (`/api/vexa/[...path]`) builds the list from **`GET /bots`** (api.v1 primary) with a **`GET /bots/status`** fallback — NOT `GET /meetings`. The carve mounted only `POST`/`DELETE /bots` in meeting-api → `GET /bots` **405**'d and `/bots/status` **404**'d → empty list. The data + `GET /meetings` existed, but the dashboard doesn't call `/meetings` for the list | **carve every endpoint the VENDORED client actually CALLS, not the ones that look canonical** — the client's real request set is the spec (same carve-gap family as `GET /auth/me`, `GET /meetings/{id}`). A backend api-surface test that hits `/meetings` proves nothing about a dashboard that lists via `/bots`; only driving the real client (or testing against its exact calls) catches it | fix: `meeting_api/collector/app.py` `GET /bots` (alias of `get_meetings`, + `has_more`) · this log |
| 31 | 06-22 | mid-run the dashboard happy path failed on transcript-persist (collector dropped the bot's flat payload) + attribution glow-leak — root-caused to the deployed **`vexaai/vexa-bot:v012` image being STALE** (predated the merged #27 transcript-format + #17 self-glow fixes, which ARE in current source). The fix required a **bot rebuild that planning never anticipated or pre-cleared** | planning validated **resources** (STT token, speaker tokens, the meeting) but never (a) the **currency** of the deployed artifacts (image vs current source) nor (b) the **operations** the run might need (rebuild / redeploy) + their capability/permission. Resource-validation ≠ operation-validation; a stale artifact and an un-pre-cleared rebuild are both **foreseeable** | **planning must pre-validate OPERATIONS + permissions like it validates resources** — validate the **verb** (build/rebuild/redeploy/restart/migrate/push: can I? may I? non-disruptive?), not just the noun; and treat **deployed-artifact currency** as a first-class preflight (a stale image is a foreseeable failure, not a surprise) | **AGENTS.md** "Plan first" (operations + permissions) + §"Where work runs" (validate operations) · this log |
| 32 | 06-22 | the bot was started as `test@vexa.ai` (user 1) and the meetings existed under user 1 (API + no-cookie proxy both returned them), yet the human's **logged-in dashboard showed ZERO meetings** | the test **identity wasn't pinned end-to-end**: the browser held a **stale login cookie from before the Hop-1 `down -v`** (its token was wiped from the rebuilt DB), so the dashboard proxy sent a dead token → empty, while the baked self-host `VEXA_API_KEY` (user 1) worked. The login flow *does* map email→user 1, but a clean stack invalidates old sessions — and none of this was a **stated setup expectation** | **pin ONE standard test identity (`test@vexa.ai`) for the whole dashboard test — the login session AND the bot-spawn token are the same user/token** (use one user unless multiple are needed); **state it in the plan** and **standardize it in the harness** (mint/look-up `test@vexa.ai`, not a random user); after a clean bring-up, re-auth the browser or use the self-host baked token. Validate "logged-in identity == spawning identity" as a preflight | plan "Test-identity setup expectation" · dashboard-harness should use `test@vexa.ai` · this log |
| 33 | 06-22 | the dashboard login **fabricated a fresh/duplicate user** for `test@vexa.ai` every time (isNewUser, zero meetings) instead of resolving the existing user 1 — even after rebuilding from current source. Module isolation proved admin-api + the dashboard's own fetch BOTH returned `{id:1}` correctly, yet the login (integration) did not | TWO stacked bugs: (a) a **carve gap** — `GET /admin/users/email/{email}` was unmounted (404) so `findUserByEmail` failed; (b) **Next.js App Router fetch caching** — `adminRequest`'s GET had no `cache:'no-store'`, so the App Router **pinned the early 404** (from before the endpoint existed) and re-served it to every login → the handler then created a user. curl hit it fresh (user 1); the cached path served stale | **never let the App Router cache identity/login fetches**: `cache:'no-store'` on admin/auth fetches + `export const dynamic='force-dynamic'` + `fetchCache='force-no-store'` + a `no-store` login response; AND carve every endpoint the vendored client calls. Debug by **module isolation vs integration** — the modules passing alone while the integration fails fingerprints a *caching/wiring* layer, not the modules | fixes: `admin_api/app/main.py` (`GET /admin/users/email/{email}`) · `vexa-admin-api.ts` (`cache:no-store`) · `send-magic-link/route.ts` (`force-dynamic`+`no-store`) · this log |
| 34 | 06-22 | spent **hours** "fixing" the dashboard the user saw — but the browser's `127.0.0.1:18030` was a **separate LOCAL Mac `vexa-dash` Docker stack** (same project name + ports), NOT bbb where the bot + every fix actually ran. The `ssh -L 18030` tunnel had **silently failed** (port already owned by the local stack), so the human's view and the work were two different systems (different users, different meetings) the entire time | I assumed "the browser tunnels to bbb" and never **verified the human's view reached the intended backend**. A same-port local stack transparently shadowed the remote one; `curl`-on-bbb (user 1) and `curl`-via-mac-tunnel (user 2) finally diverged and exposed it | **the human's actual view (browser → dashboard → which backend) is a first-class resource — VERIFY it points at the same stack where the work runs, as a preflight.** Plant a unique marker the intended backend returns (e.g. a per-stack id / a `[LOGIN-DBG]` that must appear on the target's logs) and confirm the browser hits it BEFORE trusting any UI observation. Same-port localhost stacks silently shadow tunnels — check `lsof`/`docker ps` for a local listener before assuming a tunnel | fix: stop the local stack → real tunnel to bbb → browser proven on bbb (user 1, 3 meetings, transcript rendered) · this log |
| 35 | 06-22 | the dashboard-on-bbb run became a **multi-hour cascade of execution-time discoveries** — stale bot image (#31), login App-Router cache + carve gaps (#30/#33), identity mismatch (#32), and finally a local stack shadowing bbb (#34) — each surfaced painfully mid-run, several after spending the human | the planning preflight was **thin**: it validated *resources* (ADR-0020: bbb reachable, STT, speaker tokens) but **not the rest of the execution surface** — operations/permissions, artifact currency, topology binding (which stack the human's browser actually hits), identity, and the vendored client's real endpoint set. Every failure was **foreseeable + a one-call check**, but uncovered classes have no instrument, so they detonate at execution | **a preflight is only as good as its COVERAGE — prove the WHOLE execution surface in planning, not just resources.** Consolidated the session's learnings into one canonical preflight (resources · operations+permissions · artifact-currency · topology-binding · identity · contract-coverage); an unproven line blocks plan approval. A thin preflight *guarantees* an execution-time cascade and over-spends the human (the scarce oracle becomes the probe that finds the gaps) | **AGENTS.md** §"The preflight — prove the WHOLE execution surface" (consolidates #28–#34) · this log |
| 36 | 06-22 | recording-upload `422` on the dashboard happy path. Module isolation (receiver vs bot) found TWO carve divergences: (a) the receiver required **flat `Form(...)` fields** but the bot (0.11 `RecordingService`) sends a **`metadata` JSON part** + `file` → `session_uid` (only inside metadata) was missing → 422; (b) the receiver verified a **MeetingToken** but the bot authenticates the internal upload with **`INTERNAL_API_SECRET`** (recording.ts passes `inv.internalSecret`, like the lifecycle callback) → would 401 after the form fix | the carved receiver was written to a **re-invented wire format + auth model**, not the REAL client's (the ported 0.11 bot). Same carve-gap family as `GET /bots` / email-lookup (#30/#33): the backend's shape must match what the actual client sends | **a carved receiver must match the real client's wire format AND auth model** — parse what the client sends (metadata-JSON, flat override) + accept its auth (internal secret, session-scoped). **Validate with a synthetic request in the client's EXACT format as a preflight** (here: `metadata`-form → 200 + receipt, flat-form → 200, bad-auth → 401) — no live meeting needed to prove the contract | fixes: `recordings/router.py` (parse `metadata` + internal-secret auth) · `recordings/service.py` (`token_meeting_id` optional) · this log |
| 37 | 06-22 | with a working bot in the meeting, the dashboard had **NO live WS** (transcript only on refresh). Root-caused (read-only, code) to `lib/browser-api-url.ts`: when the configured public URL (`127.0.0.1:18056`) AND the request host (`127.0.0.1`, via the SSH tunnel) are **both loopback**, `resolveBrowserApiUrl` returned same-origin (`publicApiUrl=""`) → `/api/config` `wsUrl = ws://127.0.0.1:18030/ws` (the Next.js dashboard) → **the App Router cannot upgrade a WebSocket** → dead WS | the both-loopback heuristic made two wrong assumptions for the compose/tunnel case: (a) "same-origin Next.js `/ws` carries the WS" — it doesn't (no WS proxy in the carve); (b) "the loopback port is container-internal/unreachable" — false: `:18056` is the PUBLISHED gateway host port, reachable through the tunnel | **a 'fall back to same-origin for WS' heuristic is only valid if same-origin ACTUALLY proxies the upgrade** — the App Router doesn't, so the browser must connect **gateway-direct** on the published/tunneled port. Fix: in the both-loopback branch, if the configured port == `API_GATEWAY_HOST_PORT` (the published gateway port), trust it → `wsUrl` gateway-direct. Validate the WS path end-to-end (browser → wsUrl → gateway `/ws`), not just REST | fix: `browser-api-url.ts` (both-loopback → keep configured URL when port == gateway host port) · this log |
| 38 | 06-22 | the bot's `lifecycle.v1 completed` callback **409'd 3×** and gave up → meeting stuck `active`, `meeting.completed` webhook + recording-finalize never fired. The bot returns on the first 2xx, so all 3 were 409 ⇒ the record was already illegal-from BEFORE the first `completed` | the lifecycle FSM `MeetingStore` is **in-memory + non-durable**; `apply_change` calls `get_or_create(connection_id)` with **no DB rehydration**. After ANY meeting-api restart (I rebuilt it several times) the store is empty → a fresh record at `status=None` → `can_transition(None, COMPLETED)` is False (`LEGAL_TRANSITIONS[None]={JOINING}`) → `IllegalTransition` → 409. A deploy/restart silently orphans every in-flight bot | **an FSM that gates HTTP responses must be DURABLE or rehydrate from the system of record (DB) on first touch, and terminal transitions must be IDEMPOTENT** (a duplicate/redelivered `completed` → 200, not 409). Proven live: empty store + DB=`active/stopping` → `completed` callback → 200 + DB→`completed` (no 409), reconciling the stuck meeting | fix: `lifecycle/machine.py` (`rehydrate` from DB status + idempotent no-op `StatusChange`) · `app.py` lifecycle callback · `bot_spawn/{ports,adapters,fakes}.py` (`get_status_by_session`) · `tests/test_lifecycle_durable.py` (9 proofs) · this log |
| 39 | 06-22 | the earlier WS fix (#37) made the socket **connect + subscribe + ack + ping/pong** — but STILL **zero data messages**. Two layers under it: (a) the lifecycle callback persisted to DB + delivered the webhook but **NEVER `redis.publish`**ed to `bm:meeting:{id}:status` (no status publisher existed); (b) when I added the publish I first emitted the **dashboard's** legacy `{type:'meeting.status',payload:{status}}` + mapped `needs_help→needs_human_help` IN THE CORE — the human stopped me: "bad dashboard architecture leaks into the good one" | "WS connects" ≠ "WS delivers" — connect/subscribe/ack can all be green while nothing flows (no producer). And the impedance between the clean ws.v1 contract (`bot_status`, `needs_help`) and the legacy dashboard (`meeting.status`, `needs_human_help`) must be absorbed by the **legacy client**, not the core — the core publishing the client's shape leaks legacy naming into the contract | **the CORE emits the contract; legacy clients ADAPT to it — never leak a client's legacy shape into the core.** Core publishes clean ws.v1 `bot_status` (BotStatus value verbatim); the dashboard consumes `bot_status` and maps `needs_help→needs_human_help` on ITS side. Prove WS **end-to-end** (publisher present AND consumer parses) — the adversarial-verify skeptic caught the producer-green-but-consumer-ignores gap a self-test never would | fix: `app.py` (publish clean `bot_status`) · dashboard `lib/ws-status.ts` + `use-vexa-websocket.ts` (consume `bot_status`, dashboard-side `needs_help` map) · this log |
| 40 | 06-22 | the recording **master.webm was unplayable** (`ffprobe`: "EBML header parsing failed") — bot log said "uploaded master … 1 chunk, webm", 190318 B. First bytes `43 b6 75 …` (mid-Matroska-Cluster), no EBML `1a 45 df a3` | a **read-only minio probe** localized it decisively: the stored chunk `000000.webm` was **byte-identical to the master** and itself headerless, and length was preserved 190318 B end-to-end ⇒ codec/storage/upload **innocent** (master==chunk is correct for 1 chunk; nothing dropped). The bot's blob was already headerless: the page→Node **base64 bridge dropped chunk 0** (the MediaRecorder EBML+Segment+Tracks init segment), so only cluster-only survivors reached the assembler | **localize a byte-corruption by probing the artifact at each hop BEFORE guessing the layer** (stored-chunk vs master first-bytes exonerated 3 layers in one cheap read). Fix bot-side: **retain the EBML init segment from the first self-describing blob and re-attach it to a surviving cluster-only chunk** when chunk 0's delivery is lost (chunker), with an assembler belt-and-suspenders prepend | fix: `record-chunker/src/index.ts` (retain + re-attach init segment) · `recording/src/recording-assembler.ts` (prepend guard) · `init-segment.{test,smoke.test}.ts` · this log |
| 41 | 06-22 | wiring `dashboard_new`'s Next composition root, **`next build` failed**: webpack `UnhandledSchemeError: node:fs/node:path/node:url` — the **fs-backed ajv validator was being bundled into the BROWSER**. Trace: `providers.tsx → dash-api-client → @vexa/dash-contracts → validate.js (node:fs)`. Every brick L1/L2 (tsx) had been green; only the real bundler saw it | `@vexa/dash-contracts` had **one front door mixing two runtimes** — browser-safe TYPES *and* a node-only validator that `readFileSync`s the sealed schemas — re-exported as **values** from `index`, so importing the package for a *type* dragged `node:fs` into the bundle. Compounding it, `dash-api-client` **hard-wired** `validateApiShape` to check every response — impossible in a browser regardless (the validator reads schema files from disk). tsx tests never cross the browser-bundle boundary, so they couldn't catch it | **a brick that spans runtimes must SPLIT ITS FRONT DOOR BY RUNTIME** — a **types-only `.` export** (fully erased ⇒ zero browser runtime) + a **node-only `/validate` subpath** (the fs/ajv validator); and a cross-brick **runtime** dependency must be **INJECTED (DI), not hard-wired**, so the browser path defaults to a typed pass-through (shape-drift is an L1/L2 concern, not a per-request browser cost). Corollary: the real **bundler IS the L4 gate** for the runtime/bundle boundary — a logic test can't see it | fix: `dash-contracts` (`.` types-only + `./validate` subpath export) · `dash-api-client` (injectable `validate?`, browser default = pass-through) · **AGENTS.md** (brick front door = per-runtime; inject runtime deps) · this log |
| 42 | 06-22 | the first wired-app L4 (real browser → real gateway, golden frames PUBLISHed to real redis): the WS **connected** (`gateway: WebSocket /ws [accepted]`) but **ZERO frames rendered** and the gateway never logged a subscribe/fan-in. Every `dash-ws` L2 test was green | `createWsClient.start()` registers `onOpen`/`onMessage` **then** calls `connect()`. The real `createBrowserWsTransport` attached its listeners *inside* `onOpen()`/`onMessage()` via `socket.addEventListener` — but at registration time `socket` was still `null` (connect hadn't run), so `socket?.addEventListener` **no-op'd** → the fast localhost `open` fired with no handler → the subscribe frame was never sent → no fan-in → no frames. The `FakeWsTransport` **stores** callbacks (order-independent), so it satisfied the same call order and the L2s passed — **the fake's looseness HID the adapter's ordering bug** | **a port's REAL adapter must honor the exact call-ordering contract its fake tolerates** — here: `on*` handlers may be registered BEFORE `connect()`, so the adapter must STORE them and wire them onto the socket when `connect()` creates it (never attach at registration time). A fake more permissive than the real driver hides adapter bugs (cf #27/#29). The real-browser **L4 is the regression guard** for a browser-resident adapter a fake can't model — and it caught the WS bug a self-test never could, vindicating "L4-green is what earns the human walk" | fix: `src/lib/browser-ws-transport.ts` (store the cbs, wire on `connect`) · guard: `clients/dashboard_new/e2e/live.spec.ts` (live-L4) · this log |
| 43 | 06-22 | the golden-frame L4 was green, but pointing dashboard_new at a REAL completed meeting on bbb (real prior-walk transcript + recording) exposed TWO dead #11-playback paths: (a) the `<audio>` was pointed at `/recordings/{id}/master`, which returns a **JSON descriptor**, not bytes (`first4 = 7b226964` = `{"id`) → no audio; (b) clicking a segment seeked to **~1.78e9** — the segment's ABSOLUTE unix-epoch `start` — i.e. off the end of the recording | the golden L4 injects status/transcript frames but has **no real recording** and uses **synthetic segment times**, so the data-shape-dependent paths were never exercised: the media-URL resolution (`master` → `raw_url` bytes path) and the absolute-epoch → recording-relative seek mapping. Only a real persisted artifact runs them. (The test meeting also had a duplicate native_id, mixing two sessions — so `meeting.start_time` was 60 min before the audio; the robust anchor is the earliest segment, not meeting creation.) | **an L4 over GOLDEN/synthetic data proves RENDERING, not the data-shape-dependent paths** — validate the dashboard against a REAL persisted artifact (a completed meeting's actual recording + transcript) to catch descriptor-vs-bytes and absolute-vs-relative-time bugs a synthetic frame never reveals. Resolve the recording master to its `raw_url` (the bytes), and map a segment's absolute time to a recording offset anchored on the earliest segment | fix: `src/components/meeting-detail.tsx` (master→`raw_url` audio src; segment epoch→relative seek) · guard: `clients/dashboard_new/e2e/live-playback.spec.ts` (real-data #5+#11) · this log |
| 44 | 06-22 | a human driving a LIVE gmeet bot: status came over the WS but **transcripts only appeared on REST reload, never live** — yet the socket WAS receiving `transcript` frames (browser capture: 5 frames in, rendered count frozen at the 6 REST-bootstrapped segments). My first browser-injected `transcript` frame DID render, masking it | the two-map merge's ingest gate `isValid` requires `absolute_start_time`. REST segments carry it (backend-populated); the **bot's LIVE `transcript` segments carry the time as an EPOCH `start` (`1782162716.032`) and leave `absolute_start_time` null** → every live segment filtered at ingest. The WS path (`applyTranscriptTick`) had **no normalization**, unlike the REST path (`restSegmentToLive`). My injected probe set a non-null `absolute_start_time` — an idealized fixture **more complete than the real producer's frame** — so it rendered and gave a false all-clear (same trap as #43: synthetic data fills a field the real data omits) | **the WS path must normalize like the REST path — derive `absolute_start_time` from the epoch `start` when the live segment omits it** (the client absorbs the contract impedance, ADR-0023). And: **a hand-built probe frame must mirror the REAL producer's shape, including its NULL/absent optional fields** — an over-complete fixture hides a consumer that requires what the producer omits. Capture a real frame off the wire and replay THAT, don't hand-author an idealized one | fix: `dash-meeting-state/src/transcript-merge.ts` (`normalizeLiveSeg`) · guard: `meeting-state.test.ts` (live tick: epoch start + null abs time → ingested) · verified live in-browser · this log |
| 45 | 06-22 | after #44 confirmed segments rendered live, the human reported **pending (in-progress) drafts STILL didn't render** — only `completed` ones — and directed: "must import the rendering package `packages/transcript-rendering`" | `dash-meeting-state` had **REIMPLEMENTED** the confirmed/pending two-map merge locally — "the brick owns its own copy rather than depending on the external rendering package, so the seam is the contract." But that algorithm (dedup, pending staleness, grouping, sort) is the **published `@vexaai/transcript-rendering` v0.4.x** the vendored dashboard already consumes — a reimplementation **drifts** from the canonical (here: pending handling) and duplicates battle-tested logic for no gain | **depend on the canonical published package for a non-trivial SHARED algorithm; don't reimplement it.** A brick stays decoupled by owning only its SHAPE ADAPTERS (REST/WS → the renderer's `TranscriptSegment`) + its own store/FSM — the rendering ENGINE is the package (`createTranscriptManager`). The contract seam is preserved by the adapters, not by copying the algorithm; "avoid the dep" is not a reason to fork a maintained package | fix: `dash-meeting-state` depends on `@vexaai/transcript-rendering` (`createTranscriptManager`); `transcript-merge.ts` reduced to adapters + re-export; `next.config` transpiles it · verified live in-browser (pending renders italic + confirmed) · README · this log |
| 46 | 06-22 | even with the package wired, a LIVE gmeet meeting rendered **pending-free** — and a read-only stream probe showed the bot WAS emitting drafts (22 `completed:false` vs 8 `completed:true` on `transcription_segments`), so the dashboard was twice-innocent | the bot emits each draft with `start == end` (zero duration — it has no end yet; `gmeet-pipeline.ts:93`), and the collector's `_coerce_segment` **dropped ALL zero-duration segments** (`end - start < 1e-3 → None`), a filter meant for garbage finals. So every draft was discarded two hops upstream of the UI — the symptom surfaced at the dashboard but the bug was in the collector | **a "drop degenerate rows" filter must not also drop a VALID edge case** — a pending draft legitimately has `start == end` (no end yet), so only zero-duration *completed* segments are garbage. Localize a "missing data" symptom by probing each hop's OUTPUT (the stream had drafts; the published `:mutable` had none) before blaming the consumer | fix: `collector/ingest.py` (`if completed and end-start<1e-3`) · guard: `test_ingest.py` (draft passthrough) · deployed live via the new dev loop (below) · this log |
| 47 | 06-22 | hand-patching the deployed source on `bbb` to ship a fix was the wrong model — it was correctly BLOCKED (modifying shared infra in place) and wouldn't survive a container recreate; deploys had been ad-hoc snapshot edits + image rebuilds the whole session | the bbb deploy was a **plain snapshot copy** (`/home/dima/v012-mock`, pre-reorg `meetings/` layout), not a git checkout — so there was no sync path from the repo, and every fix meant a manual edit + a full rebuild (and a stale image, #31, kept biting) | **deploy via a SYNCED git CLONE + hot-reload DEV CONTAINERS, not in-place edits of a detached snapshot.** Clone the repo on the host (`git pull` = sync); run each service with its **source mounted over the image** + a file-watcher (`watchfiles` restart for the python entrypoint) so `git pull` → reload, **no rebuild**. The loop becomes: edit → push → `git pull` on host → watcher restarts → live | `deploy/compose/docker-compose.dev.yml` (all 4 python services: src mount + watchfiles); bbb clone `/home/dima/vexa-0.12` @ 0.12; local `.env`/`docker-compose.stt.yml` carried over (gitignored) · this log |
| 48 | 06-22 | building the bot from the synced clone (for its dev loop) FAILED — the bot Dockerfile still `COPY meetings/runtime/gateway/...` (flat) + `cd /app/meetings/...`, and `COPY sdks` 404'd (a git clone omits empty dirs) | the `core/` reorg moved the SOURCE + updated `pnpm-workspace.yaml`, but **left the bot's BUILD on the old layout** — its Dockerfile/entrypoint were never exercised against `core/` because the bot was only ever built from the pre-reorg snapshot. Also: `clients/dashboard_new` (npm workspace) landed under the `clients/*` pnpm glob, which `pnpm install` can't resolve | **a directory reorg must migrate the BUILD CONTEXTS (Dockerfiles, entrypoints, copy paths), not just the source + workspace manifest** — a build only proven from a detached snapshot hides the drift. And COPY only dirs that exist in a clone (empty workspace globs are absent); exclude foreign-toolchain dirs from the workspace glob | fix: bot `Dockerfile` (`COPY core ./core` + scripts; core/ paths) · `entrypoint.sh` (`cd /app/core/meetings/services/bot`) · `pnpm-workspace.yaml` (`!clients/dashboard_new`) · `deploy/bin/redeploy-bot.sh` (fast app-layer rebuild) — bot image rebuilt green from the clone · this log |
| 49 | 06-23 | a maturity campaign adversarially testing the four meeting-api seams (lifecycle · webhooks+backoff · api · robustness) found **11 real bugs in a system that had just passed its full happy-path walk + looked mature** — incl. 2 HIGH concurrency races (max-bots cap TOCTOU over-provision; dedup TOCTOU double-spawn), an unhandled-input 500 (unknown `platform` → uncaught jsonschema error), a silent `bool("false")==True` coercion, a missing webhook dead-letter queue, a doubled first backoff, an un-isolated collector publish, a partial-spawn orphan, no-Range recordings, and a no-op envelope double-count | happy-path + structural-green (L1–L3) testing never exercises the ADVERSARIAL edges: true concurrency (TOCTOU), hostile input TYPES/enum values, dependency faults (publish raises), delivery-guarantee exhaustion (retry/DLQ), and partial-failure compensation. "It works + is mature" was true for the demonstrated path and false for the edges right next to it | **maturity is earned by PER-SEAM ADVERSARIAL testing, not more happy-path** — fault-inject (raise on publish/DB), force concurrency (`asyncio.gather` + yield-modeling fakes that mimic real-DB await suspension), feed hostile types/values, and exhaust the retry/backoff/DLQ — and BANK each as a standing regression suite. The efficient autonomous shape: parallel seam-agents (find→author tests→report) → a synthesis pass (fix at source → deploy via the dev loop → L4-verify live). Concurrency fixes need the REAL driver to verify (advisory lock / unique index ⇒ live Postgres; the fake only proves the service USES the atomic primitive) | 5 seam suites (~206 tests) + `docs/MATURITY-FINDINGS.md`; fixes batched `657036b`/`cc4badd`/`6600e45`, A1/A2/R1/ROB2 L4-verified on bbb · this log |
| 50 | 06-23 | the human reported: **POST a bot then immediately DELETE it → an ORPHAN bot stays in the meeting** (DB says `stopping`, a real bot is live in the call). "A symptom of a serious design gap." | the stop path only PUBLISHED a fire-and-forget `bot_commands:meeting:{id}` `{action:leave}` over redis PUB/SUB and trusted the bot to self-leave. redis pub/sub has **no buffering for late subscribers** — a DELETE right after POST runs while the bot is still BOOTING (status `requested`, not yet subscribed) → the leave is **dropped** → the bot joins and is never told to leave → orphan. The teardown REQUESTED an effect over an unreliable channel instead of GUARANTEEING it | **a stop/teardown must GUARANTEE the effect, not request it over a channel the target may not be listening on.** Pair the graceful path (a command for a CONFIRMED-listening consumer, so it finalizes cleanly — here an `active` bot leaves + finalizes its recording) with a HARD guarantee (directly kill the workload when the consumer can't be confirmed listening — here a booting bot), and RECONCILE the create/destroy race (the spawn re-checks for a stop that landed mid-boot, before the workload id was even written). Generalizes: fire-and-forget to a not-yet-subscribed consumer is a lost message, every time | fix: `lifecycle/stop_router.py` (B1 direct `runtime.delete_workload` for a booting bot) + `bot_spawn/service.py` (C spawn re-checks `get_status_by_session` → tears down a raced stop) · `test_robustness_seam.py` (2 tests) · committed `9cbd45c`, **L4-verified live on bbb** (POST→DELETE → no orphan container) · follow-up B2: reconcile loop kills the workload of stale-`stopping` meetings (backstop for an active bot that misses the leave) · this log |
| 51 | 06-27 | live transcript "flickers / drops lines" (output → lost → reappear) → I proposed a **TERMINAL-side workaround** (monotonic-pending upsert in `meetingLive.ts`) to mask it | the flicker is **introduced upstream** in the gmeet lane: `speaker-streams.ts:handleTranscriptionResult` emits the pending draft **twice per Whisper submission** — the tail (`currentWords.slice(prefixLen)`, ~line 286) AND the whole draft (`trimmed`, ~line 349) — both keyed `segment_id = key:round(windowStartMs)` (gmeet-pipeline.ts:93). One id alternates full↔fragment; collector + relay faithfully carry it; the terminal upsert-by-id flickers. I conflated *where the symptom surfaces* (terminal) with *where it's owned/introduced* (the producer) — and the same producer feeds the dashboard, which flickers identically | **fix at the point of INTRODUCTION, not OBSERVATION** — trace observation→origin hop-by-hop before choosing the fix site; never compensate in a consumer for a producer's defect (we don't work around our own bugs). Localized by recording all three hops: source `transcription_segments` → `:mutable` → SSE all carried the alternation, so the lane is the origin | sharpened AGENTS.md hard-rule **"Fix at the point of INTRODUCTION, not OBSERVATION"**; source fix pending in `speaker-streams.ts` (emit the pending draft ONCE) · this log |
