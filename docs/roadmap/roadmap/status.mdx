---
title: "Status"
description: "Where we are — honestly."
---

<Note>This page is the honest, current state — updated as we go, never aspirational.</Note>

**Proven in production (pre-existing Vexa):** the [Runtime](/primitives/runtime) (meeting bots are browser
workloads), transcription → `transcript.v1`, redis [streaming](/architecture/streaming).

**Designed & frozen (this site):** the [seven primitives](/primitives/overview), the
[identity layer](/architecture/identity-and-trust) (kagenti-aligned), the
[governance model](/architecture/governance).

**In rebuild:** the **execution core** — moving the agent OUT of the control plane into runtime-spawned,
isolated, generic-agent containers (the earlier in-process slices were reverted; see
[approach](/roadmap/approach)).

**Sound and kept:** the terminal workbench + surfaces, the generic event ingress, the generic tool
mechanism, the redis scheduler.

**Next:** draw the contracts against the frozen primitives, then build the execution core (stage 1).
