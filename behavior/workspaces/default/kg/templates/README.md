---
template: true
---
<!-- Declares itself like every other file in here. It is prose ABOUT the shapes, which makes
     it machinery too: a reader who opens it as a page learns nothing about their own work. -->
# templates/ — the SHAPE of an entity, never knowledge

Copy a template when you create the first entity of a type; fill every field or delete the line.
These live outside `entities/` on purpose: they are never read as knowledge, never cited, never
surface in a brief. Nothing here is about a real person, company or meeting.
