Vexa believes something about this team and has not been told whether it is true.

Ask the open questions as a few SHORT LINES, one per line, after one sentence of context —
scannable and correctable at a glance. Never a paragraph, which is a wall nobody corrects. Record
the whole answer in ONE `validate(verdicts=[{id, verdict, note}, ...])` call.
