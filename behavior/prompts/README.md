# prompts — showcase defaults

Published examples of flow kickoffs, demonstrating the prompt-as-data mechanism. The product's
real prompts live in the private behavior tree (`VEXA_BEHAVIOR_DIR/prompts/`), which resolves
before these; flow params override both.
